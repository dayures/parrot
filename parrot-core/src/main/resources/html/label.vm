#foreach($label in $element.labels)
<span class="parrot-label"><span class="parrot-label-text">$label.text</span>
  #if ($label.locale)
  <span class="parrot-label-locale">[$label.locale]</span>
  #end
</span>#if ($foreach.hasNext), #end
#end

