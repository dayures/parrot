#foreach($label in $element.synonyms)
<span class="parrot-label">
<span class="parrot-label-text #if($label.isAltLabel()) parrot-label-text-alt #end">$label.text</span>
  #if ($label.locale)
  <span class="parrot-label-locale">[$label.locale]</span>
  #end
</span>#if ($foreach.hasNext), #end
#end	
